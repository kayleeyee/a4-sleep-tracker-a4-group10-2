import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'a4-sleep-tracker-a4-group10-2',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
